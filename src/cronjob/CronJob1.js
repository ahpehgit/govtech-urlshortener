const CronJob1 = async(dependencies) => {

    //const encryptionService = dependencies.EncryptionService;
    //const { facilityRepository } = dependencies.DBService;
    console.log('Running CronJob1...');
}

module.exports = CronJob1